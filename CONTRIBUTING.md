# ライブラリ整備ガイド

この文書では、競技プログラミング用ライブラリの追加・修正から、検証、ドキュメント公開までの流れをまとめます。
実装だけでなく、検証コードとドキュメントを一組として整備してください。

## 前提

- C++23（`g++ -std=gnu++23`）での利用を想定します。
- ライブラリはheader-onlyとし、カテゴリごとのディレクトリに配置します。
- 検証コードは同じカテゴリの`test/`、ドキュメントは`docs/`に配置します。
- 添字は原則0-based、区間は原則として半開区間`[l, r)`を使用します。

たとえば文字列アルゴリズムは次の構成です。

```text
string/
├── algorithm.hpp
├── test/
│   └── algorithm.test.cpp
└── docs/
    ├── algorithm.md
    └── index.md
```

## 実装方針

### 競技プログラミングでの使いやすさ

実装では、次の点を優先します。

- 十分に速い漸近計算量
- 小さい定数倍
- 予測しやすいメモリ使用量
- コンテスト中に扱いやすい単純で明示的なAPI
- よく知られ、実戦で検証されているアルゴリズムやデータ構造

巧妙さよりも、正しさを説明しやすく、制約内で安定して動作する実装を選んでください。

### ヘッダ

カテゴリ直下に自己完結したヘッダを置きます。

```cpp
#pragma once

// 必要な標準ライブラリだけをincludeする
```

次の点を確認します。

- `#pragma once`を付ける。
- 必要な標準ライブラリをヘッダ自身でincludeする。
- ライブラリヘッダでは`std::`を明記し、includeした側の名前空間を汚さない。
- グローバルなマクロや一般的すぎる名前を不用意に追加しない。
- 近隣のヘッダに合わせて、命名、テンプレート引数、返り値、公開メンバの方針を決める。
- コンストラクタ、更新、取得を単純で明示的な操作として提供する。
- 不要な動的確保、再帰、コピーを避け、制約に対するメモリ使用量を確認する。

### 公開APIと前提条件

公開APIには、コンテスト中に誤用しにくいインターフェースを選びます。

- 添字は原則0-based、区間は原則として半開区間`[l, r)`にする。
- 添字、区間、構築状態など、呼び出し側が守るべき前提条件は、近隣の実装に合わせて`assert`で検査する。
- 検索失敗など通常起こり得る結果は、`-1`、`false`、`std::optional`などの返り値として表現する。
- `const`メソッドでは、キャッシュなどを含む隠れた状態変更を原則行わない。
- 永続データ構造の更新では旧バージョンを保持し、新しいバージョンを返すことをAPIとドキュメントで明示する。

`assert`はデバッグ時にプログラマの誤用を検出するためのものです。オンラインジャッジの入力検証や、正常に起こり得る失敗の処理には使用しません。

既存の公開APIを変更する場合は、利用箇所、検証コード、ドキュメントを検索し、互換性への影響を確認してください。

## 検証

### 検証先の選択

ライブラリに対応する問題がある場合は、次の順で検証先を選びます。

1. Library Checker
2. AOJ
3. yukicoder

Library Checkerを使う場合は、必要に応じて
[公式問題リポジトリ](https://github.com/yosupo06/library-checker-problems/tree/master)も確認してください。
問題メタデータ、生成器、入力検証器、出力検証器、例、参照解などから、制約や入出力の詳細を確認できます。

AOJとyukicoderを使う場合は、公式問題ページを正本として、制約、入出力、ジャッジ形式を確認してください。

AtCoderは`oj-verify`で安定して検証できないため、新しい検証先には使用しません。

### 検証コード

検証コードは同じカテゴリの`test/`に置き、ファイル名を`*.test.cpp`にします。オンラインジャッジの問題を使う場合は、ファイル先頭に問題URLを指定します。

```cpp
#define PROBLEM "https://example.com/problem"

#include <iostream>
#include "../algorithm.hpp"

int main() {
    // 入力、ライブラリの呼び出し、出力
}
```

ファイル名には、必要に応じて検証先や用途を含めます。

```text
algorithm.test.cpp
algorithm.aoj.test.cpp
algorithm.random.test.cpp
```

ジャッジ問題だけでは確認しにくい性質がある場合は、その性質を直接確認する`assert`やランダムテストを追加します。
実用的な範囲で愚直解と比較し、次のようなケースを確認してください。

- 空入力と最小サイズ
- 最大値付近と境界値
- 重複する値や同じ操作の繰り返し
- 単調列、全要素が同じ列、偏ったグラフなどの退化ケース
- 更新順序や構築前後など、状態を持つ構造の各段階

適切なライブラリ固有の問題がない場合に限り、`aplusb`を検証実行の入口として使用できます。
その場合も、検証対象の性質を直接確認する`assert`や、愚直解とのランダム比較を追加してください。
既存の`aplusb`検証を監査するときは、対応するLibrary Checker問題があれば置き換えます。

## ドキュメント

### 配置とfront matter

ドキュメントは同じカテゴリの`docs/`に置き、対応するヘッダをfront matterで指定します。

````markdown
---
title: Algorithm
documentation_of: ../algorithm.hpp
---

何を求めるライブラリかを簡潔に説明します。

## 使い方

```cpp
auto result = algorithm(input);
```
````

新しい公開ヘッダを追加したら、同じカテゴリの`docs/index.md`にもリンクを追加してください。新しいカテゴリを追加する場合は、ルートの`README.md`からも見つけられるようにします。

### 記載内容

ドキュメントは、ソースを読まなくても利用方法を判断できる内容にします。少なくとも次を記載してください。

- 何を解決するライブラリかを示す短い概要
- テンプレート引数に必要な型や演算
- モノイドなどの代数的な前提
- 主な操作を示すコンパクトな使用例
- 正確な公開インターフェースのシグネチャ
- 各コンストラクタと公開メソッドの時間計算量
- 構造全体と、必要に応じて各操作の空間計算量
- 引数、返り値、添字、区間、失敗条件に関する注意
- 更新が破壊的か、新しいバージョンを返すか、時刻を進めるかなどの状態変化
- 検証に使用した問題へのリンク、または実施したランダムテストの概要

複数の公開メソッドがある場合は、次のような表を使用してシグネチャと計算量を対応させます。

```markdown
| シグネチャ | 説明 | 計算量 |
| --- | --- | --- |
| `explicit Structure(int n)` | サイズ`n`で構築する | `O(n)` |
| `Value query(int l, int r) const` | 区間`[l, r)`を取得する | `O(log n)` |
```

### JekyllとLiquid

ドキュメントはJekyllとGitHub Pagesで公開されるため、front matterを持つMarkdown本文ではLiquid構文が有効です。Markdownのコードブロック内もLiquidの処理対象になります。

Markdown本文の例にLiquidの区切りと衝突する記述が必要な場合は、該当部分をLiquidの`raw`ブロックで
保護するなど、Jekyllで正しく生成できる形にしてください。ドキュメント生成ツールがデータとして埋め込む
ヘッダや検証ソースについて、C++の初期化構文を一律に禁止する必要はありません。

## ローカル確認

まず対象の検証だけを実行します。

```sh
oj-verify run string/test/algorithm.test.cpp --jobs 1 --timeout 3600
```

コンパイル設定は`.verify-helper/config.toml`にあり、現在は`g++ -std=gnu++23 -O2 -Wall -Wextra`を使用します。警告も確認してください。

ドキュメント生成を確認します。

```sh
oj-verify docs --jobs 1
```

最後に差分と不要な生成物がないことを確認します。

```sh
git diff --check
git status --short
```

`oj-verify all`は問題データの取得、コンパイル、実行を一括で行うため、環境によって負荷が高くなります。ローカルでは対象ファイルを個別に検証し、全体検証が必要な場合も`--jobs 1`から始めてください。

```sh
oj-verify all --jobs 1 --timeout 3600
```

## CIとドキュメント公開

GitHub Actionsは`main`へのpush、または手動実行で動作します。Pull Requestをトリガーにはしていません。

CIでは次の処理を行います。

1. 検証対象とドキュメントを解析する。
2. 検証を複数のランナーへ分割する。
3. 結果を統合して成否を判定する。
4. ドキュメントを生成し、Jekyllでビルドする。
5. GitHub Pagesへデプロイする。

以前の検証結果はキャッシュされます。結果を再利用したくない場合は、Actionsの手動実行で`ignore_prev_result`を有効にします。

生成されたドキュメントやタイムスタンプをbotからリポジトリへ自動コミットする運用にはしていません。

## フォーマット

VS Codeでは`.vscode/settings.json`のclang-format設定を使用します。現在の主な設定は次のとおりです。

- LLVMベース
- インデント幅4
- 1行120文字
- includeを並べ替えない
- 制御文へ`{}`を挿入する

検証コード（`*.test.cpp`）では、includeの後に`using namespace std;`を置きます。ライブラリヘッダでは使用せず、`std::`を明記します。

フォーマット後は意味のない大規模差分が混ざっていないか確認します。既存ファイルを修正するときは、目的と無関係な箇所まで一括整形しないでください。

## 変更時の注意

- コードだけでなく、対応する検証とドキュメントも更新する。
- ファイルを移動した場合は、相対include、`documentation_of`、索引のリンクを確認する。
- 検証先のURL、制約、入出力、ジャッジ形式が正しいことを確認する。
- CI設定を変更するときは、権限、キャッシュ、artifact、Pages公開への影響を確認する。
- Git管理外のファイルや生成物を誤って追加しない。
- 既存の未コミット変更を消したり、目的外の変更を混ぜたりしない。

## 完了の目安

次を満たしたら整備完了です。

- ヘッダ単体で必要な依存関係が解決される。
- APIの前提条件、添字、区間、状態変化が実装とドキュメントで一致している。
- 対応する検証が成功し、必要な境界値やランダムテストが含まれている。
- ドキュメントから公開API、計算量、主な使い方を判断できる。
- ドキュメントがJekyllで生成できる。
- コンパイラ警告やフォーマット上の問題がない。
- 差分が意図したファイルと内容に限定されている。
- `main`への反映後、GitHub ActionsとGitHub Pagesが正常である。
